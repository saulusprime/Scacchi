# Dipendenze di sviluppo (test e lint). Installa anche le dipendenze dei servizi:
#   pip install -r backend/requirements.txt -r frontend/requirements.txt -r requirements-dev.txt
pytest>=9.1.1
ruff>=0.6
httpx>=0.27

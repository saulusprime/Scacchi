fastapi>=0.110
uvicorn[standard]>=0.29
SQLAlchemy>=2.0
alembic>=1.18.5
python-dotenv>=1.0
httpx>=0.27
Pillow>=10.0
anthropic>=0.40
# Cifratura dei token provider nel DB (Fernet); licenza Apache-2.0/BSD.
cryptography>=42
# KittenTTS: sintesi vocale locale (ONNX, solo CPU) per la sezione di istruzione
# guidata. E' il submodule git in integrazioni/KittenTTS: installare dalla ROOT del
# repository (come fa `make install`) dopo `git submodule update --init`.
./integrazioni/KittenTTS
# Piper TTS (voci ITALIANE del servizio /tts) e' una dipendenza OPZIONALE e non
# viene installata di default: licenza GPL-3 (il progetto e' MIT), l'abilitazione
# e' una scelta esplicita dell'operatore:  pip install piper-tts
# Senza, /tts risponde 503 per l'italiano e l'inglese (KittenTTS) resta attivo.
